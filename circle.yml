machine:
  post:
    - pyenv global 2.7.11 3.5.2

dependencies:
  override:
    - pip install -r ansible/requirements.txt
    - pip install tox==2.5.0

test:
  override:
    - ./test.sh

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh
